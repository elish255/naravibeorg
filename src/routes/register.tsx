import {createFileRoute,useNavigate} from "@tanstack/react-router";
import {useState} from "react"; import {useServerFn} from "@tanstack/react-start"; import {registerUser} from "@/lib/app.functions";
export const Route=createFileRoute("/register")({component:Register});
function Register(){const nav=useNavigate();const reg=useServerFn(registerUser);const [form,setForm]=useState({name:"",username:"",email:"",phone:"",country:"Tanzania",password:"",confirm:""});const [err,setErr]=useState("");const submit=async(e:React.FormEvent)=>{e.preventDefault();if(form.password!==form.confirm)return setErr("Passwords hazifanani.");try{await reg({data:{name:form.name,username:form.username,email:form.email,phone:form.phone,country:form.country,password:form.password}});await nav({to:"/payment"})}catch(e){setErr(e instanceof Error&&e.message==="USERNAME_EXISTS"?"Username tayari imetumika.":e instanceof Error&&e.message==="EMAIL_OR_PHONE_EXISTS"?"Email au namba tayari imesajiliwa.":"Usajili umeshindikana.")}};return <main className="min-h-screen bg-background p-4"><div className="mx-auto max-w-lg py-6"><div className="rounded-3xl bg-card p-5 shadow-card"><h1 className="text-2xl font-black">Jisajili — NARAVIBE</h1><p className="mt-1 text-sm text-muted-foreground">Tengeneza akaunti yako kisha malizia activation.</p><form onSubmit={submit} className="mt-5 space-y-3">{[["name","Full Name"],["username","Username"],["phone","Phone Number"],["email","Email Address"],["country","Country"]].map(([k,l])=><input key={k} required value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})} placeholder={l} className="w-full rounded-xl border bg-background p-3"/>)}<input required type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Password" className="w-full rounded-xl border bg-background p-3"/><input required type="password" value={form.confirm} onChange={e=>setForm({...form,confirm:e.target.value})} placeholder="Confirm Password" className="w-full rounded-xl border bg-background p-3"/>{err&&<p className="text-sm font-semibold text-destructive">{err}</p>}<button className="w-full rounded-xl bg-primary py-3.5 font-bold text-primary-foreground">JISAJILI</button><button type="button" onClick={()=>void nav({to:"/login"})} className="w-full py-2 text-sm underline">Tayari nina account? Login</button></form></div></div></main>}
