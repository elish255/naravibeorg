import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { useState } from "react";
import { Eye, EyeOff, ArrowLeft, LogIn } from "lucide-react";
import { useServerFn } from "@tanstack/react-start";
import { loginUser } from "@/lib/app.functions";
export const Route=createFileRoute("/login")({component:LoginPage,head:()=>({meta:[{title:"Ingia — NARAVIBE"},{name:"robots",content:"noindex,nofollow"}]})});
function LoginPage(){const navigate=useNavigate();const login=useServerFn(loginUser);const [username,setUsername]=useState("");const [password,setPassword]=useState("");const [show,setShow]=useState(false);const [error,setError]=useState("");const [loading,setLoading]=useState(false);async function submit(e:React.FormEvent){e.preventDefault();setError("");setLoading(true);try{const r=await login({data:{username,password}});await navigate({to:r.status==="active"?"/dashboard":"/payment"});}catch(e){const m=e instanceof Error?e.message:"";setError(m==="BANNED"?"Akaunti yako imefungwa.":m==="INACTIVE"?"Akaunti yako imezuiwa kwa muda.":"Username au password si sahihi.");}finally{setLoading(false)}}return <main className="min-h-screen bg-background px-4 py-8"><div className="mx-auto max-w-md"><Link to="/" className="inline-flex items-center gap-2 text-sm font-semibold text-primary"><ArrowLeft className="h-4 w-4"/> Rudi mwanzo</Link><div className="mt-4 rounded-3xl bg-card p-6 shadow-card"><img src="/favicon.png" alt="NARAVIBE" className="mx-auto h-20 w-20 rounded-2xl object-contain"/><div className="mx-auto mt-2 flex h-12 w-12 items-center justify-center rounded-full bg-primary text-primary-foreground"><LogIn className="h-6 w-6"/></div><h1 className="mt-3 text-center text-2xl font-extrabold">Ingia kwenye Account</h1><form onSubmit={submit} className="mt-6 space-y-4"><input required value={username} onChange={e=>setUsername(e.target.value.toLowerCase())} placeholder="Username" className="w-full rounded-xl bg-secondary px-4 py-3 text-sm outline-none"/><div className="relative"><input required type={show?"text":"password"} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password" className="w-full rounded-xl bg-secondary px-4 py-3 pr-11 text-sm outline-none"/><button type="button" onClick={()=>setShow(v=>!v)} className="absolute right-3 top-1/2 -translate-y-1/2 text-muted-foreground">{show?<EyeOff className="h-4 w-4"/>:<Eye className="h-4 w-4"/>}</button></div>{error&&<p className="rounded-xl bg-destructive/10 p-3 text-sm font-semibold text-destructive">{error}</p>}<button disabled={loading} className="w-full rounded-full bg-primary py-3.5 font-bold text-primary-foreground">{loading?"INAINGIA...":"INGIA"}</button><p className="text-center text-xs text-muted-foreground">Huna account? <Link to="/register" className="font-bold text-primary">Jisajili</Link></p></form></div></div></main>}
